import 'package:flutter/material.dart';

/// Defines the color palette for the Brun UI.
abstract class AppColors {
  static const Color primary = Color(0xFF0B2C51);
  static const Color secondary = Color(0xFF87AEFF);
  static const Color dark = Color(0xFF1E2E43);

  static const Color transparent = Colors.transparent;
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color gray = Color(0xFFF1F1F3);
}
