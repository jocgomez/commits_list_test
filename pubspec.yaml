name: commits_list_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  riverpod: ^2.4.0
  intl: ^0.18.0
  google_fonts: ^6.1.0
  go_router: ^13.1.0
  dartz: ^0.10.1
  dio: ^5.4.0
  envied: ^0.5.3
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  very_good_analysis: ^5.0.0
  envied_generator: ^0.5.3
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
